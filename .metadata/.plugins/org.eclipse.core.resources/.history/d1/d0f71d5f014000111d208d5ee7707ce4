class PagesController < ApplicationController
  
  def show
    @page = Page.find(params[:id])
        
    @title = "#{@page.title}"
      
  end
  
  def show
    @service = Service.find(params[:id])
        
    @title = "#{@service.title}"
      
  end
end
